using UnityEditor;
using UnityEngine;
using BLK10.Singleton;


public static class ToolboxMenu
{
    /*
    [MenuItem("Assets/BLK10/Toolbox/Show", true, 0)]
    public static bool ValidateShowToolbox()
    {
        return (true);
    }
    
    [MenuItem("Assets/BLK10/Toolbox/Show")]
    public static void ShowToolbox()
    {
        IToolbox tool = Toolbox.Instance;
        if (tool != null)
            tool.hideFlags = HideFlags.None;
    }


    [MenuItem("Assets/BLK10/Toolbox/Hide", true, 1)]
    public static bool ValidateHideToolbox()
    {
        return (true);
    }
    
    [MenuItem("Assets/BLK10/Toolbox/Hide")]
    public static void HideToolbox()
    {
        IToolbox tool = Toolbox.Instance;
        if (tool != null)
            tool.hideFlags = HideFlags.HideInHierarchy | HideFlags.HideInInspector | HideFlags.NotEditable;        
    }
    */
}
